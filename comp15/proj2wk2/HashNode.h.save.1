
#include <bits/stdc++.h>
#include <vectors>
using namespace std;

#ifndef HASH_NODE_H_
#define HASH_NODE_H_

template<typename K, typename V>
class HashNode
{
public:
    //V value;
    K key;
    vector<V> values;

    HashNode(K key, vector<V> values){
        this->values = values;
        //this->values.push_back(value)
        this->key = key;
    }

};

#endif
